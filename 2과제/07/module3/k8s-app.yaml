apiVersion: v1
kind: Namespace
metadata:
  name: skillsmkt
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: order-processor-sa
  namespace: skillsmkt
  annotations:
    eks.amazonaws.com/role-arn: "${APP_IRSA_ROLE_ARN}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-processor
  namespace: skillsmkt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-processor
  template:
    metadata:
      labels:
        app: order-processor
    spec:
      serviceAccountName: order-processor-sa
      # Pod 종료가 지연되면 노드 반환도 늦어지므로 grace period 를 짧게 둔다.
      terminationGracePeriodSeconds: 10
      tolerations:
        - key: "skm-app-nodepool"
          operator: "Exists"
          effect: "NoSchedule"
      nodeSelector:
        karpenter.sh/nodepool: skm-app-nodepool
      containers:
        - name: order-processor
          image: "${ECR_IMAGE}"
          ports:
            - containerPort: 8080
          env:
            - name: AWS_REGION
              value: "ap-northeast-2"
            - name: SQS_QUEUE_URL
              value: "${SQS_QUEUE_URL}"
            - name: PROCESSING_TIME
              value: "3"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
